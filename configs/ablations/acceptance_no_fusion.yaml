# Ablation: acceptance routing without fusion (concept note §10.4,
# plan todo 22). Combines acceptance-only routing with sequential per-branch
# verification.

name: acceptance_no_fusion
description: "Acceptance-only routing AND no candidate fusion (combined ablation)."
overrides:
  max_experts: 2
  routing_mode: acceptance
  tree_verify: false
  no_target_embedding: false
  no_online_feedback: false
