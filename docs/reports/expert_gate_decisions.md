# Expert gate decisions (plan todo 25)

Gate rule: add expert iff oracle per-mechanism coverage recovers >= 5.0pp additional positions with expected acceptance >= 1.0 token in >=1 task class (configs/expert_gates.yaml, committed before this todo ran).

## reasoning_transition: SKIP

- no per-mechanism oracle coverage >= 5.0pp with expected acceptance >= 1.0 (best additional_pct=0.0); evidence pointer: docs/reports/oracle_report.md

## logit_dynamics: SKIP

- no per-mechanism oracle coverage >= 5.0pp with expected acceptance >= 1.0 (best additional_pct=0.0); evidence pointer: docs/reports/oracle_report.md

Evidence source: `docs/reports/oracle_report.md` and `runs/oracle/verdict.json`.