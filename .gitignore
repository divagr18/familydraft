# Python junk
__pycache__/
*.py[cod]
.venv/
dist/
build/
.ruff_cache/
.pytest_cache/
*.egg-info/

# data/ and runs/ artifacts (keep the directory markers)
data/*
!data/.gitkeep
runs/*
!runs/.gitkeep

# secrets and editor
.env
.vscode/

# agent-harness session metadata (churns every turn; not planning state)
.omo/run-continuation/

# agent tooling indexes (large, local only)
.codegraph/
